# Global aliases: expanded anywhere on the command line, not just at the start.
# Useful for redirection shortcuts, e.g. `cmd NUL` -> `cmd >/dev/null 2>&1`.

alias -g NE='2>/dev/null'
alias -g DN='>/dev/null'
alias -g NUL='>/dev/null 2>&1'
